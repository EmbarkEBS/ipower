<odoo>
    <record id="view_order_form_inherit_internal_cost" model="ir.ui.view">
        <field name="name">sale.order.form.internal.cost</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Global Charges Tab -->
            <xpath expr="//page[@name='order_lines']" position="after">
                <page string="Internal Charges" name="internal_charges">
                    <group>
                        <field name="freight"/>
                        <field name="duty"/>
                        <field name="misc"/>
                        <field name="total_extra" readonly="1"/>
                    </group>
                </page>
            </xpath>

            <!-- Show the Internal Charge next to the Price -->
            <xpath expr="//field[@name='order_line']//list//field[@name='price_unit']" position="after">
                <field name="internal_charge" string="Int. Charge" optional="show"/>
            </xpath>

            <!-- ENSURE Price Unit is NOT readonly -->
            <xpath expr="//field[@name='order_line']//list//field[@name='price_unit']" position="attributes">
                <attribute name="readonly">0</attribute>
            </xpath>

        </field>
    </record>
</odoo>
