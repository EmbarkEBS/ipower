<odoo>
    <record id="view_order_form_inherit_internal_cost" model="ir.ui.view">
        <field name="name">sale.order.form.internal.cost</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Internal Charges Tab -->
            <xpath expr="//page[@name='order_lines']" position="after">
                <page string="Internal Cost" name="internal_charges" groups="sales_team.group_sale_manager">
                    <group>
                        <group>
                            <field name="freight"/>
                            <field name="duty"/>
                            <field name="misc"/>
                        </group>
                        <group>
                            <field name="total_extra" readonly="1"/>
                        </group>
                    </group>
                    <p class="text-muted">Calculated as: Product Cost + (Total Extra / Total Quantity). Tax applies only to Product Cost.</p>
                </page>
            </xpath>

            <!-- Set Price Unit to Readonly since it's calculated from Cost + Extra -->
            <xpath expr="//field[@name='order_line']//list//field[@name='price_unit']" position="attributes">
                <attribute name="readonly">1</attribute>
                <attribute name="force_save">1</attribute>
            </xpath>

        </field>
    </record>
</odoo>
