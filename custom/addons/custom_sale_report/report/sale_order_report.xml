<odoo>

    <!-- PAPER FORMAT -->

  <record id="paperformat_custom_po"
        model="report.paperformat">

    <field name="name">Custom PO Paperformat</field>
    <field name="default" eval="False"/>

    <field name="format">A4</field>
    <field name="orientation">Portrait</field>

    <!-- reserve space for header -->
    <field name="margin_top">8</field>

    <!-- reserve space for footer -->
    <field name="margin_bottom">8</field>

    <field name="margin_left">10</field>
    <field name="margin_right">10</field>

    <field name="header_line" eval="False"/>
    <field name="header_spacing">10</field>

    <field name="dpi">90</field>

</record>

    <!-- REPORT -->

    <record id="action_report_custom_sale_order"
            model="ir.actions.report">

        <field name="name">Quotation / Sales Order</field>

        <field name="model">sale.order</field>

        <field name="report_type">qweb-pdf</field>

       <field name="report_name">custom_sale_report.sale_order_template</field>

    <field name="report_file">custom_sale_report.sale_order_template</field>

    <field name="binding_model_id" ref="sale.model_sale_order"/>

    <field name="binding_type">report</field>

    <field name="print_report_name">'Sales Order - %s' % (object.name)</field>

        <field name="paperformat_id"
               ref="custom_sale_report.paperformat_custom_po"/>

       

    </record>

</odoo>