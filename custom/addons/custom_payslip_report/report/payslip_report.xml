<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- PAPER FORMAT -->
  <record id="paperformat_a5_payslip" model="report.paperformat">
    <field name="name">A5 Payslip</field>
    <field name="default">False</field>

    <!-- A5 Format -->
    <field name="format">A5</field>

    <field name="orientation">Portrait</field>

    <!-- Margins -->
    <field name="margin_top">5</field>
    <field name="margin_bottom">5</field>
    <field name="margin_left">5</field>
    <field name="margin_right">5</field>

    <field name="header_line">False</field>
    <field name="header_spacing">0</field>
    <field name="dpi">96</field>
</record>
    <record id="action_custom_payslip_report"
            model="ir.actions.report">

        <field name="name">Custom Payslip</field>

        <field name="model">hr.payslip</field>

        <field name="report_type">qweb-pdf</field>

          <field name="report_name">custom_payslip_report.custom_payslip_template</field>
    <field name="report_file">custom_payslip_report.custom_payslip_template</field>

        <field name="binding_model_id"
               ref="hr_payroll.model_hr_payslip"/>

        <field name="binding_type">report</field>
        <field name="print_report_name">'Payslip- %s' % (object.name)</field>
<field name="paperformat_id" ref="custom_payslip_report.paperformat_a5_payslip"/>
    </record>

</odoo>