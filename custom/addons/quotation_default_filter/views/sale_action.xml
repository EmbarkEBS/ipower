<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale.action_quotations_with_onboarding"
            model="ir.actions.act_window">

        <field name="domain">
            [('user_id', '=', uid), ('state', 'in', ['draft', 'sent'])]
        </field>

    </record>

</odoo>
