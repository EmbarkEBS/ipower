<odoo>
    <record id="view_product_form_lead_time_weeks" model="ir.ui.view">
        <field name="name">product.template.form.lead.time.weeks.safe</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">

            <!-- Insert inside Logistics group -->
            <xpath expr="//group[.//field[@name='weight']]" position="inside">
                <div class="o_row">
                    <label for="lead_time_weeks"/>
                    <field name="lead_time_weeks"/>
                    <span class="ms-2">weeks</span>
                </div>
            </xpath>

        </field>
    </record>
</odoo>