<odoo>
    <record id="lead_time_weeks_view_int" model="ir.ui.view">
        <field name="name">product.template.lead.time.weeks.int</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">

            <!-- Remove default lead time UI safely -->
            <xpath expr="//field[@name='sale_delay']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <!-- Add integer weeks field -->
            <xpath expr="//group[.//field[@name='weight']]" position="inside">
                <div class="o_row">
                    <label for="lead_time_weeks"/>
                    <field name="lead_time_weeks"/>
                    <span class="ms-2">weeks</span>
                </div>
            </xpath>

        </field>
    </record>
</odoo>